#include <bits/stdc++.h>
using namespace std;
int main()
{
    stack<int> s;
    s.push(10);
    s.push(80);
    cout<<s.top();
    s.pop();
    cout<<endl<<s.top();
    if(s.empty()==true)
        cout<<endl<<"no element";
    else
        cout<<endl<<"there are elements";

    return 0;
}
